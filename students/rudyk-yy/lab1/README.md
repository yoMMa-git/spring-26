# Лабораторная работа №1

## Задание

1. выбрать датасет для классификации, например на [kaggle](https://www.kaggle.com/datasets?tags=13302-Classification);
   1. датасет должен содержать пропуски;
   2. датасет должен содержать категориальные и количественные признаки;
2. реализовать алгоритм построения дерева ID3 с критерием Джини;
3. реализовать обработку пропущенных значений через оценку вероятности;
4. обучить дерево на выбранном датасете;
5. оценить качество классификации;
6. реализовать алгоритм редукции дерева;
7. сравнить качество классификации и регрессии до и после редукции дерева;
8. сравнить с [эталонной](https://scikit-learn.org/stable/) реализацией бинарного решающего дерева;
   1. сравнить качество работы;
9. подготовить небольшой отчет о проделанной работе.

## Отчёт

### 1. Датасет

В качестве датасета выбран [Titanic Survival Prediction Dataset](https://www.kaggle.com/datasets/yasserh/titanic-dataset)

Данный набор данных содержит информацию о пассажирах Титаника, включая их возраст, пол, класс билета, количество родственников на борту и другие признаки.
Целевая переменная — **Survived** (1 — пассажир выжил, 0 — не выжил).

Основная цель — построить **дерево решений ID3 с критерием Джини**, которое будет предсказывать выживание пассажира по смешанным признакам.

#### Подготовка данных:

- Загружен CSV-файл `titanic.csv` по URL;
- Использованы как числовые, так и категориальные признаки;
- Пропуски в признаках `Age` и `Embarked` сохранены и обрабатываются алгоритмом во время обучения и предсказания;
- Датасет разделён на обучающую, валидационную и тестовую выборки.

```python
import csv
import urllib.request

url = "https://raw.githubusercontent.com/datasciencedojo/datasets/master/titanic.csv"
response = urllib.request.urlopen(url, timeout=10)
raw_data = response.read().decode("utf-8")
rows = list(csv.DictReader(raw_data.splitlines()))

def to_float(value):
    if value is None or value == "":
        return None
    return float(value)

X = []
y = []
for row in rows:
    X.append([
        int(row["Pclass"]),
        row["Sex"],
        to_float(row["Age"]),
        int(row["SibSp"]),
        to_float(row["Fare"]),
        row["Embarked"] if row["Embarked"] else None,
    ])
    y.append(int(row["Survived"]))
```

Используемые признаки:
- `Pclass` — числовой;
- `Sex` — категориальный;
- `Age` — числовой, содержит пропуски;
- `SibSp` — числовой;
- `Fare` — числовой;
- `Embarked` — категориальный, содержит пропуски.

Размер датасета: `891` объектов.

Распределение классов:
- `Did not survive`: `549` объектов (`61.6%`);
- `Survived`: `342` объекта (`38.4%`).

## 2. Программная реализация

Структура проекта

```
lab1/
├── source/
│ └── model.py      # Реализация дерева решений
└── README.md
```

В программе реализовано согласно заданию:

* **Построение дерева ID3 с критерием Джини** — [код](source/model.py)


* **Поддержка категориальных признаков** — [код](source/model.py)


* **Обработка пропущенных значений через оценку вероятности** — [код](source/model.py)


* **Reduced Error Pruning** — [код](source/model.py)


## 3. Анализ и сравнение

В рамках эксперимента были рассмотрены:
- обучение полного дерева;
- обработка пропущенных значений;
- прунинг дерева с помощью REP;
- сравнение собственной реализации с `sklearn`.

### Обработка пропущенных значений

При обучении дерева на данных с искусственно добавленными пропусками была получена точность:

```
Accuracy: 0.6011
```

Пример объекта с пропусками:

```python
[3, 'male', None, 0, 7.25, None]
```

Предсказание модели:

```
Did not survive
```

### Полное дерево

Статистика дерева до прунинга:

```
Количество узлов: 982
Количество листьев: 520
Максимальная глубина: 11
```

Метрики на тестовой выборке:

```
Accuracy: 0.7416
Precision: 0.7477
Recall: 0.7169
F1-Score: 0.7320
```

Матрица ошибок:

```
[[90 13]
 [33 42]]
```

### Прунинг дерева

До прунинга:

```
Узлов: 982, Листьев: 520, Глубина: 11
Accuracy train: 0.9065
Accuracy val:   0.7865
Accuracy test:  0.7416
```

После редукции:

```
Узлов: 52, Листьев: 28, Глубина: 10
Accuracy train: 0.8355
Accuracy val:   0.8427
Accuracy test:  0.7247
```

Матрица ошибок после редукции:

```
[[87 16]
 [33 42]]
```

Сокращение размера дерева:

```
Сокращение количества узлов: 94.7%
Сокращение количества листьев: 94.6%
```



### Сравнение с эталоном

При сравнении своей реализации:
```python
DecisionTree(max_depth=10)
```

С реализацией sklearn:

```python
DecisionTreeClassifier(
    criterion='gini',
    max_depth=10,
    min_samples_split=2,
    min_samples_leaf=1,
    random_state=42,
)
```

Получены следующие результаты:

```
                          Наша (до ред.)   Наша (после ред.)   sklearn
Листьев                   520              28                  121
Глубина                   11               10                  20
Accuracy                  0.7416           0.7247              0.7247
Precision (macro)         0.7477           0.7246              0.7179
Recall (macro)            0.7169           0.7023              0.7186
F1-Score (macro)          0.7320           0.7133              0.7182
```

Подробный отчёт sklearn:

```
                 precision    recall  f1-score   support

Did not survive       0.76      0.76      0.76       103
       Survived       0.67      0.68      0.68        75

       accuracy                           0.72       178
      macro avg       0.72      0.72      0.72       178
   weighted avg       0.73      0.72      0.72       178
```

Видно, что полное дерево в собственной реализации показывает лучшую точность на тесте, чем дерево после прунинга и результат sklearn на данном разбиении. После прунинга дерево становится значительно компактнее, но часть качества теряется.


## Вывод

В ходе лабораторной работы было реализовано дерево решений ID3 с критерием Джини для задачи предсказания выживания пассажиров Титаника.


